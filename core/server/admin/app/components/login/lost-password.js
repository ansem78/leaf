define(['angular'],function(angular) {

  var app = angular.module('Leaf');

  app.register.controller('lostpasswordController',['$rootScope','$scope','$http',function($rootScope,$scope,$http) {



    // Imposta una nuova password per l'utente corrispondente a un indirizzo e-mail.
    $scope.setNewPassword = function() {

    };



  }]);

});